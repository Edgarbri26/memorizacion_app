import 'package:flutter/material.dart';
import 'package:memorizacion_app/app.dart';
// void main() {
//   runApp(const MemoApp());
// }

void main() {
  // eso es para que la app se ejecute en pantalla completa y no se vea la barra de estado
  // WidgetsFlutterBinding.ensureInitialized();
  // SystemChrome.setEnabledSystemUIMode(SystemUiMode.immersiveSticky);
  // runApp(const MyApp());
    runApp(const MemoApp());
}